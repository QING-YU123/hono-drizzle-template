{
  "permissions": {
    "allow": [
      "Bash(claude skills:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)",
      "Bash(cd D:\\\\Projects\\\\ModelTemplateProject\\\\hono-drizzle-template:*)",
      "Bash(npm view:*)",
      "Bash(cat:*)",
      "Bash(cat > \"D:/Projects/ModelTemplateProject/hono-drizzle-template/business/iterations/v0.1.0-MVP/测试/测试用例.md\" << 'ENDOFFILE'\n# 用户认证与管理 API 测试用例\n\n**版本**: v0.1.0-MVP\n**生成日期**: 2026-03-02\n**基于文档**: openapi.yaml\n**API 版本**: v0.1.0-MVP\n\n---\n\n## 📋 目录\n\n- [测试概要]\\(#测试概要\\)\n- [API 端点清单]\\(#api-端点清单\\)\n- [测试用例详情]\\(#测试用例详情\\)\n  - [认证模块]\\(#认证模块\\)\n  - [用户管理模块]\\(#用户管理模块\\)\n- [集成测试]\\(#集成测试\\)\n- [边界条件测试]\\(#边界条件测试\\)\n- [测试环境要求]\\(#测试环境要求\\)\n\n---\n\n## 测试概要\n\n### API 信息\n- **API 名称**: 用户认证与管理 API\n- **API 版本**: v0.1.0-MVP\n- **API 描述**: 基于验证码的用户注册和密码登录功能，提供完整的用户认证和管理功能\n\n### 测试统计\n- **API 端点数**: 10 个\n- **测试用例总数**: 58 个\n  - 功能正确性: 12 个 ✅\n  - 异常处理: 35 个 ❌\n  - 边界条件: 8 个 ⚠️\n  - 数据一致性: 3 个 🔄\n\n### 测试覆盖范围\n\n| 模块 | 端点 | 测试用例数 |\n|------|------|------------|\n| 认证 | POST /auth/register | 8 |\n| 认证 | POST /auth/login | 7 |\n| 认证 | POST /auth/refresh | 5 |\n| 认证 | POST /auth/logout | 3 |\n| 用户管理 | GET /api/users | 3 |\n| 用户管理 | POST /api/users | 8 |\n| 用户管理 | GET /api/users/:id | 5 |\n| 用户管理 | PUT /api/users/:id | 8 |\n| 用户管理 | DELETE /api/users/:id | 5 |\n| 用户管理 | GET /user/me | 3 |\n| 集成测试 | - | 2 |\n| 边界条件测试 | - | 3 |\n\n---\n\n## API 端点清单\n\n| 方法 | 路径 | 操作ID | 描述 | 测试用例数 |\n|------|------|--------|------|------------|\n| POST | /auth/register | register | 用户注册 | 8 |\n| POST | /auth/login | login | 用户登录 | 7 |\n| POST | /auth/refresh | refresh | 刷新访问令牌 | 5 |\n| POST | /auth/logout | logout | 注销登录 | 3 |\n| GET | /api/users | getAllUsers | 获取所有用户 | 3 |\n| POST | /api/users | createUser | 创建用户 | 8 |\n| GET | /api/users/:id | getUserById | 获取单个用户 | 5 |\n| PUT | /api/users/:id | updateUser | 更新用户 | 8 |\n| DELETE | /api/users/:id | deleteUser | 删除用户 | 5 |\n| GET | /user/me | getCurrentUser | 获取当前用户 | 3 |\n\n---\n\n## 测试用例详情\n\n### 认证模块\n\n#### POST /auth/register - 用户注册\n\n**测试场景分析：**\n- 成功场景：注册成功、返回完整用户对象\n- 异常场景：缺少必填字段、邮箱格式无效、密码太短、邮箱已存在、JSON 格式错误\n- 边界条件：密码刚好 8 字符、不提供 name 参数\n- 数据一致性：注册后可通过邮箱查询用户、邮箱唯一性约束\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-AUTH-REG-001 | 注册成功（含 name） | 无 | 1. 发送 POST 请求到 `/auth/register`<br>2. 请求体包含合法的 email、password（8+字符）、name | 1. 响应状态码为 201<br>2. 返回完整 User 对象（含 id、is_admin 等）<br>3. 所有字段值与请求一致 |\n| TC-AUTH-REG-002 | 注册成功（不含 name） | 无 | 1. 发送 POST 请求到 `/auth/register`<br>2. 请求体包含 email、password，不含 name | 1. 响应状态码为 201<br>2. name 字段为邮箱前缀 |\n| TC-AUTH-REG-003 | 密码边界值（8 字符） | 无 | 1. 发送 POST 请求，密码刚好 8 字符 | 1. 响应状态码为 201<br>2. 注册成功 |\n| TC-AUTH-REG-004 | 缺少 email 字段 | 无 | 1. 发送 POST 请求，不含 email 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"email is required\" |\n| TC-AUTH-REG-005 | 缺少 password 字段 | 无 | 1. 发送 POST 请求，不含 password 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"password is required\" |\n| TC-AUTH-REG-006 | 邮箱格式无效 | 无 | 1. 发送 POST 请求，email 格式无效（如 \"invalid-email\"） | 1. 响应状态码为 400<br>2. 错误信息提示 \"email must be a valid email address\" |\n| TC-AUTH-REG-007 | 密码少于 8 字符 | 无 | 1. 发送 POST 请求，password 为 7 字符 | 1. 响应状态码为 400<br>2. 错误信息提示 \"password must be at least 8 characters\" |\n| TC-AUTH-REG-008 | 邮箱已存在 | 数据库中已存在该邮箱 | 1. 发送 POST 请求，使用已存在的邮箱 | 1. 响应状态码为 409<br>2. 错误信息提示 \"Email already exists\" |\n\n#### POST /auth/login - 用户登录\n\n**测试场景分析：**\n- 成功场景：登录成功、返回令牌和用户信息\n- 异常场景：缺少必填字段、邮箱或密码错误、JSON 格式错误\n- 边界条件：邮箱大小写处理、空字符串处理\n- 数据一致性：登录后返回的用户信息与数据库一致、令牌格式正确\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-AUTH-LOG-001 | 登录成功 | 用户已注册 | 1. 发送 POST 请求到 `/auth/login`<br>2. 使用正确的 email 和 password | 1. 响应状态码为 200<br>2. 返回 accessToken（JWT 格式）<br>3. 返回 refreshToken<br>4. 返回 user 信息（不含密码） |\n| TC-AUTH-LOG-002 | 缺少 email 字段 | 无 | 1. 发送 POST 请求，不含 email 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"email is required\" |\n| TC-AUTH-LOG-003 | 缺少 password 字段 | 无 | 1. 发送 POST 请求，不含 password 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"password is required\" |\n| TC-AUTH-LOG-004 | 邮箱为空字符串 | 无 | 1. 发送 POST 请求，email 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"email is required\" |\n| TC-AUTH-LOG-005 | 密码为空字符串 | 无 | 1. 发送 POST 请求，password 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"password is required\" |\n| TC-AUTH-LOG-006 | 用户不存在 | 无 | 1. 发送 POST 请求，使用不存在的邮箱 | 1. 响应状态码为 401<br>2. 错误信息提示 \"Invalid email or password\"（不区分邮箱不存在和密码错误） |\n| TC-AUTH-LOG-007 | 密码错误 | 用户已注册 | 1. 发送 POST 请求，使用错误的密码 | 1. 响应状态码为 401<br>2. 错误信息提示 \"Invalid email or password\" |\n\n#### POST /auth/refresh - 刷新访问令牌\n\n**测试场景分析：**\n- 成功场景：刷新成功、返回新令牌对\n- 异常场景：缺少令牌、令牌无效、令牌过期\n- 边界条件：令牌即将过期、令牌刚过期\n- 数据一致性：旧刷新令牌失效、新令牌可正常使用\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-AUTH-REF-001 | 刷新成功 | 用户已登录 | 1. 发送 POST 请求到 `/auth/refresh`<br>2. 使用有效的 refreshToken | 1. 响应状态码为 200<br>2. 返回新的 accessToken<br>3. 返回新的 refreshToken（令牌轮换） |\n| TC-AUTH-REF-002 | 缺少 refreshToken 字段 | 无 | 1. 发送 POST 请求，不含 refreshToken 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"refreshToken is required\" |\n| TC-AUTH-REF-003 | refreshToken 为空字符串 | 无 | 1. 发送 POST 请求，refreshToken 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"refreshToken is required\" |\n| TC-AUTH-REF-004 | 刷新令牌无效 | 无 | 1. 发送 POST 请求，使用无效的 refreshToken | 1. 响应状态码为 401<br>2. 错误信息提示 \"Invalid or expired refresh token\" |\n| TC-AUTH-REF-005 | 刷新令牌已使用过 | 令牌已使用过一次 | 1. 发送 POST 请求，使用已刷新过的 refreshToken | 1. 响应状态码为 401<br>2. 错误信息提示 \"Invalid or expired refresh token\" |\n\n#### POST /auth/logout - 注销登录\n\n**测试场景分析：**\n- 成功场景：注销成功、会话被删除\n- 异常场景：缺少令牌\n- 边界条件：重复注销（幂等性）\n- 数据一致性：注销后无法使用该刷新令牌\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-AUTH-LOGOUT-001 | 注销成功 | 用户已登录 | 1. 发送 POST 请求到 `/auth/logout`<br>2. 使用有效的 refreshToken | 1. 响应状态码为 200<br>2. 返回 \"Logged out successfully\" |\n| TC-AUTH-LOGOUT-002 | 缺少 refreshToken 字段 | 无 | 1. 发送 POST 请求，不含 refreshToken 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"refreshToken is required\" |\n| TC-AUTH-LOGOUT-003 | 重复注销（幂等性） | 无 | 1. 第一次发送注销请求<br>2. 第二次使用同一 refreshToken 注销 | 两次都返回 200<br>操作幂等 |\n\n---\n\n### 用户管理模块\n\n#### GET /api/users - 获取所有用户\n\n**测试场景分析：**\n- 成功场景：获取成功、返回用户列表\n- 异常场景：无特殊异常\n- 边界条件：空列表、单条记录\n- 数据一致性：不包含已软删除的用户\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-GET-001 | 获取成功（多条记录） | 数据库中有多条用户记录 | 1. 发送 GET 请求到 `/api/users` | 1. 响应状态码为 200<br>2. 响应体为 JSON 数组<br>3. 每个元素包含必需字段<br>4. 不包含已软删除的用户 |\n| TC-USER-GET-002 | 获取成功（空列表） | 数据库中无用户记录 | 1. 发送 GET 请求到 `/api/users` | 1. 响应状态码为 200<br>2. 响应体为空数组 |\n| TC-USER-GET-003 | 软删除过滤 | 数据库中有已软删除的用户 | 1. 软删除一个用户<br>2. 发送 GET 请求到 `/api/users` | 1. 响应状态码为 200<br>2. 已软删除的用户不在结果中 |\n\n#### POST /api/users - 创建用户\n\n**测试场景分析：**\n- 成功场景：创建成功、返回完整用户对象\n- 异常场景：缺少必填字段、字段为空字符串、邮箱已存在、JSON 格式错误\n- 边界条件：name 字段最大长度\n- 数据一致性：创建后可通过 GET 获取、邮箱唯一性约束\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-POST-001 | 创建成功 | 无 | 1. 发送 POST 请求到 `/api/users`<br>2. 请求体包含 name、email | 1. 响应状态码为 201<br>2. 返回完整 User 对象<br>3. id 字段自动生成<br>4. is_admin 默认为 false |\n| TC-USER-POST-002 | 缺少 name 字段 | 无 | 1. 发送 POST 请求，不含 name 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"name is required\" |\n| TC-USER-POST-003 | name 为空字符串 | 无 | 1. 发送 POST 请求，name 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"name is required\" |\n| TC-USER-POST-004 | 缺少 email 字段 | 无 | 1. 发送 POST 请求，不含 email 字段 | 1. 响应状态码为 400<br>2. 错误信息提示 \"email is required\" |\n| TC-USER-POST-005 | email 为空字符串 | 无 | 1. 发送 POST 请求，email 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"email is required\" |\n| TC-USER-POST-006 | 邮箱已存在 | 数据库中已存在该邮箱 | 1. 发送 POST 请求，使用已存在的邮箱 | 1. 响应状态码为 409<br>2. 错误信息提示 \"Email already exists\" |\n| TC-USER-POST-007 | JSON 格式错误 | 无 | 1. 发送 POST 请求，请求体不是有效 JSON | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid JSON body\" |\n| TC-USER-POST-008 | 创建后可查询 | 用户已创建 | 1. 发送 POST 请求创建用户<br>2. 发送 GET 请求到 `/api/users/{id}` | 第二步能获取到创建的用户信息 |\n\n#### GET /api/users/:id - 获取单个用户\n\n**测试场景分析：**\n- 成功场景：获取成功、返回用户详情\n- 异常场景：ID 格式无效、用户不存在\n- 边界条件：ID = 1、ID = 最大值\n- 数据一致性：软删除用户返回 404\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-GETID-001 | 获取成功 | 用户已存在 | 1. 发送 GET 请求到 `/api/users/{id}` | 1. 响应状态码为 200<br>2. 返回完整 User 对象 |\n| TC-USER-GETID-002 | ID 格式无效（字符串） | 无 | 1. 发送 GET 请求，ID 为字符串（如 \"abc\"） | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid id\" |\n| TC-USER-GETID-003 | ID 为 0 | 无 | 1. 发送 GET 请求，ID 为 0 | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid id\" |\n| TC-USER-GETID-004 | ID 为负数 | 无 | 1. 发送 GET 请求，ID 为 -1 | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid id\" |\n| TC-USER-GETID-005 | 用户不存在 | 无 | 1. 发送 GET 请求，使用不存在的用户 ID | 1. 响应状态码为 404<br>2. 错误信息提示 \"User not found\" |\n\n#### PUT /api/users/:id - 更新用户\n\n**测试场景分析：**\n- 成功场景：更新成功、部分更新、返回更新后对象\n- 异常场景：ID 格式无效、用户不存在、字段为空字符串、邮箱已存在、没有字段需要更新\n- 边界条件：只更新一个字段、更新所有字段\n- 数据一致性：更新后 GET 返回新值、updated_at 已更新\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-PUT-001 | 更新 name 字段 | 用户已存在 | 1. 发送 PUT 请求到 `/api/users/{id}`<br>2. 只更新 name 字段 | 1. 响应状态码为 200<br>2. name 已更新<br>3. 其他字段保持不变<br>4. updated_at 已更新 |\n| TC-USER-PUT-002 | 更新 email 字段 | 用户已存在 | 1. 发送 PUT 请求到 `/api/users/{id}`<br>2. 只更新 email 字段 | 1. 响应状态码为 200<br>2. email 已更新 |\n| TC-USER-PUT-003 | 同时更新多个字段 | 用户已存在 | 1. 发送 PUT 请求，同时更新 name 和 email | 1. 响应状态码为 200<br>2. 所有指定字段已更新 |\n| TC-USER-PUT-004 | ID 格式无效 | 无 | 1. 发送 PUT 请求，ID 格式无效 | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid id\" |\n| TC-USER-PUT-005 | 用户不存在 | 无 | 1. 发送 PUT 请求，用户不存在 | 1. 响应状态码为 404<br>2. 错误信息提示 \"User not found\" |\n| TC-USER-PUT-006 | 邮箱已存在 | 其他用户使用该邮箱 | 1. 发送 PUT 请求，更新为已存在的邮箱 | 1. 响应状态码为 409<br>2. 错误信息提示 \"Email already exists\" |\n| TC-USER-PUT-007 | 没有字段需要更新 | 无 | 1. 发送 PUT 请求，请求体为空对象 {} | 1. 响应状态码为 400<br>2. 错误信息提示 \"Nothing to update\" |\n| TC-USER-PUT-008 | 字段为空字符串 | 无 | 1. 发送 PUT 请求，name 为空字符串 | 1. 响应状态码为 400<br>2. 错误信息提示 \"name must be a non-empty string\" |\n\n#### DELETE /api/users/:id - 删除用户\n\n**测试场景分析：**\n- 成功场景：删除成功、软删除验证\n- 异常场景：ID 格式无效、用户不存在\n- 边界条件：重复删除\n- 数据一致性：删除后无法查询、deleted_at 已设置\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-DEL-001 | 删除成功（软删除） | 用户已存在 | 1. 发送 DELETE 请求到 `/api/users/{id}` | 1. 响应状态码为 200<br>2. 返回 \"Deleted\" 消息和被删除的用户信息<br>3. deleted_at 字段已设置 |\n| TC-USER-DEL-002 | ID 格式无效 | 无 | 1. 发送 DELETE 请求，ID 格式无效 | 1. 响应状态码为 400<br>2. 错误信息提示 \"Invalid id\" |\n| TC-USER-DEL-003 | 用户不存在 | 无 | 1. 发送 DELETE 请求，用户不存在 | 1. 响应状态码为 404<br>2. 错误信息提示 \"User not found\" |\n| TC-USER-DEL-004 | 删除后无法查询 | 用户已删除 | 1. 发送 GET 请求到已删除的用户 ID | 1. 响应状态码为 404<br>2. 错误信息提示 \"User not found\" |\n| TC-USER-DEL-005 | 重复删除 | 用户已删除 | 1. 第二次发送 DELETE 请求到同一用户 ID | 1. 响应状态码为 404<br>2. 错误信息提示 \"User not found\" |\n\n#### GET /user/me - 获取当前登录用户信息\n\n**测试场景分析：**\n- 成功场景：获取成功、返回当前用户信息\n- 异常场景：未携带令牌、令牌无效、令牌过期\n- 边界条件：令牌即将过期\n- 数据一致性：返回用户信息与令牌中的 userId 一致\n\n| 测试ID | 测试点 | 前置条件 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|----------|\n| TC-USER-ME-001 | 获取成功 | 用户已登录 | 1. 发送 GET 请求到 `/user/me`<br>2. Authorization 头包含有效的 accessToken | 1. 响应状态码为 200<br>2. 返回完整 User 对象<br>3. 用户信息与令牌中的 userId 一致 |\n| TC-USER-ME-002 | 未携带 Authorization 头 | 无 | 1. 发送 GET 请求到 `/user/me`<br>2. 不携带 Authorization 头 | 1. 响应状态码为 401<br>2. 错误信息提示 \"Unauthorized\" |\n| TC-USER-ME-003 | 令牌无效 | 无 | 1. 发送 GET 请求到 `/user/me`<br>2. Authorization 头包含无效的 accessToken | 1. 响应状态码为 401<br>2. 错误信息提示 \"Unauthorized\" |\n\n---\n\n## 集成测试\n\n| 测试ID | 测试点 | 测试步骤 | 预期结果 |\n|--------|--------|----------|----------|\n| TC-INT-001 | 完整认证流程 | 1. POST `/auth/register` 注册用户<br>2. POST `/auth/login` 登录<br>3. GET `/user/me` 验证登录状态<br>4. POST `/auth/refresh` 刷新令牌<br>5. POST `/auth/logout` 注销 | 每一步操作都成功，令牌轮换正常 |\n| TC-INT-002 | 用户管理 CRUD 流程 | 1. POST `/api/users` 创建用户<br>2. GET `/api/users/{id}` 查询创建的用户<br>3. PUT `/api/users/{id}` 更新用户信息<br>4. GET `/api/users/{id}` 验证更新<br>5. DELETE `/api/users/{id}` 删除用户<br>6. GET `/api/users/{id}` 验证删除 | 每一步操作都成功，数据一致性保持 |\n\n---\n\n## 边界条件测试\n\n| 测试ID | 测试点 | 测试场景 | 预期结果 |\n|--------|--------|----------|----------|\n| TC-BND-001 | 字符串长度边界 | name 字段值为 255 字符（最大长度） | 成功创建/更新，字段值为最大长度 |\n| TC-BND-002 | 邮箱格式边界 | 邮箱包含特殊字符（如 user+test@example.com） | 正确处理，验证通过 |\n| TC-BND-003 | Unicode 字符 | name 字段包含 Unicode 字符（如中文、表情符号） | 正确处理和存储 |\n\n---\n\n## 测试环境要求\n\n### 数据库要求\n- 需要独立的测试数据库环境\n- 数据库表结构应符合 OpenAPI 定义\n- 每个测试用例执行前重置数据库状态\n- 建议使用事务回滚机制确保数据隔离\n\n### 测试数据准备\n- 每个测试用例应独立准备测试数据\n- 测试完成后应清理数据\n- 或使用事务回滚机制\n\n### 认证要求\n- 部分接口需要有效的访问令牌\n- 测试前需要先注册并登录获取令牌\n- 测试刷新令牌时需要有效的登录会话\n\n### 环境变量\n- `DATABASE_URL`: 测试数据库连接字符串\n- `JWT_SECRET`: JWT 签名密钥（至少 32 字符）\n\n---\n\n## 附录\n\n### OpenAPI 规范摘要\n- **OpenAPI 版本**: 3.0.3\n- **服务器**: http://localhost:3001\n- **认证方式**: Bearer JWT\n- **响应格式**: JSON\n\n### 数据模型\n\n#### User（用户信息）\n```typescript\n{\n  id: number;           // 用户 ID\n  name: string;         // 用户名称\n  email: string;        // 用户邮箱（唯一）\n  is_admin: boolean;    // 是否为管理员\n  created_at: string;   // 创建时间\n  updated_at: string    // 更新时间\n}\n```\n\n#### RegisterRequest（注册请求）\n```typescript\n{\n  email: string;        // 用户邮箱（必填，有效邮箱格式）\n  password: string;     // 密码（必填，最少 8 字符）\n  name?: string;        // 用户名称（可选）\n}\n```\n\n#### LoginRequest（登录请求）\n```typescript\n{\n  email: string;        // 用户邮箱（必填）\n  password: string;     // 密码（必填）\n}\n```\n\n#### LoginResponse（登录响应）\n```typescript\n{\n  accessToken: string;  // JWT 访问令牌（15 分钟）\n  refreshToken: string; // 刷新令牌（7 天）\n  user: {\n    id: number;\n    email: string;\n    name: string;\n  }\n}\n```\n\n### 错误码清单\n\n| HTTP 状态码 | 错误代码 | 错误消息示例 | 说明 |\n|-----------|---------|-------------|------|\n| 400 | VALIDATION_ERROR | email is required / password must be at least 8 characters | 请求参数验证失败 |\n| 401 | UNAUTHORIZED | Invalid email or password / Unauthorized | 未认证或令牌无效 |\n| 404 | NOT_FOUND | User not found | 资源不存在 |\n| 409 | CONFLICT | Email already exists | 邮箱已存在 |\n\n---\n\n*生成时间: 2026-03-02*\n*基于文档: openapi.yaml*\nENDOFFILE)",
      "Bash(printf %s\\\\n:*)",
      "Bash(node:*)",
      "Bash(cd \"D:\\\\Projects\\\\ModelTemplateProject\\\\hono-drizzle-template\" && pnpm test -- --reporter=verbose 2>&1)",
      "Bash(cd \"D:\\\\Projects\\\\ModelTemplateProject\\\\hono-drizzle-template\" && pnpm test -- --run 2>&1)",
      "Bash(cd \"D:\\\\Projects\\\\ModelTemplateProject\\\\hono-drizzle-template\" && pnpm add jsonwebtoken && pnpm add -D @types/jsonwebtoken 2>&1)",
      "Bash(cd \"D:\\\\Projects\\\\ModelTemplateProject\\\\hono-drizzle-template\" && pnpm exec tsx --eval \"const pg = require\\('pg'\\); const client = new pg.Client\\({connectionString: process.env.DATABASE_URL}\\); client.connect\\(\\).then\\(\\(\\) => console.log\\('DB OK'\\)\\).catch\\(e => console.error\\('DB Error:', e.message\\)\\)\" 2>&1)"
    ]
  }
}
